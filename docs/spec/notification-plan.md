# 알림 기능 구현 계획

> 헥사고날 아키텍처 + DDD 기반. `chat-implementation-plan.md` / `vote-implementation-tasks.md` 패턴 계승.
> `BaseEntity` / `BaseTimeEntity` / `TimeTrackable` / `ClockConfig` 는 V2 (chat) 명세에서 정의된 것을 그대로 재사용.
>
> 본 문서는 PR 단위 작업 지시서다. 한 STEP을 끝낼 때마다 `./gradlew build` 통과 + 해당 STEP 테스트 통과를 확인한 후 다음 STEP으로 진행한다.

---

## 0. 사전 정보

### 0-1. 프로젝트 컨텍스트

- 프로젝트: **VS 앱 (Ject 백엔드)**
- 본 작업의 도메인: **Notification Bounded Context** (신규)
- 의존 도메인: **Vote BC** (`VoteEndedEvent`, `VoteParticipationRepository`), **Chat BC**의 공통 기반 (`BaseEntity` 등)
- 기술 스택: Java 25, Spring Boot 3.5, Spring Data JPA, PostgreSQL, Flyway, Lombok
- 아키텍처: **헥사고날 + DDD**
- 베이스 패키지: `com.ject.vs`

### 0-2. 코드 컨벤션 (vote 명세와 동일)

- 도메인 객체는 정적 팩토리 메서드로 생성 (`Notification.of(...)`, `NotificationSetting.createDefault(...)`). `new` 직접 사용 금지
- 도메인 검증은 도메인 객체 내부에서 수행
- Spring Data JPA 인터페이스는 `domain` 패키지에 둔다
- 비즈니스 로직 인터페이스(UseCase)는 `port/in` 패키지에 둔다
- UseCase 구현체는 `port` 패키지에 둔다
- 외부 시스템 추상화 인터페이스는 `port/out` 에 두고, 구현체는 `adapter/out` 에 둔다 (Web Push 발송)
- DB 시간 컬럼은 `Instant` (UTC 저장). 도메인 객체도 `Instant`. 응답 직렬화 시점에만 `OffsetDateTime` (KST `+09:00`)로 변환
- 시간 비교 로직은 `Clock` 주입받아 사용
- Lombok 사용 가능. 단 `@Setter`는 엔티티에 붙이지 말 것
- `@Transactional`은 Service 클래스 단위, 조회 메서드는 `readOnly = true`

### 0-3. Vote 도메인과의 의존 관계

알림 도메인은 다음 두 가지 방식으로 Vote 도메인을 의존한다:

1. **VoteEndedEvent listen** — `vote/event/VoteEndedEvent` 를 `@EventListener`로 수신
2. **VoteParticipationRepository** — 특정 vote에 참여한 회원 user_id 목록 조회

추가로 alarm 트리거 판정용 카운트 조회 메서드 1개가 필요해서 `VoteQueryUseCase`에 `countParticipationByUserId(Long userId)` 추가가 필요하다 (STEP 5에서 vote 측에 PR 분리 또는 본 PR에 포함 여부 결정).

### 0-4. 본 PR 범위 외 (재확인)

- iOS 16.4 미만 안내, 인앱 브라우저(카카오톡/인스타그램) 미지원 안내, A2HS 설치 유도 모달, OS 권한 팝업 → **모두 프론트 영역**
- Service Worker 코드 (`navigator.serviceWorker.register(...)`, `push` / `notificationclick` 핸들러) → **프론트 영역**
- 알림 종류 다양화 (광고 / 운영 / 친구 활동 등) → 본 PR은 `VOTE_RESULT_PUBLISHED` 단일

---

## 1. 패키지 구조

```
com.ject.vs
├── common/domain                       # (chat 명세에서 정의됨, 재사용)
│   ├── BaseEntity
│   ├── BaseTimeEntity
│   └── TimeTrackable
│
├── notification                        # Notification Bounded Context (신규)
│   ├── domain
│   │   ├── Notification.java                   # extends BaseEntity (createdAt만 직접 관리, 도메인 의미 명확)
│   │   ├── NotificationType.java               # enum
│   │   ├── NotificationSetting.java            # extends BaseTimeEntity
│   │   ├── PushSubscription.java               # extends BaseTimeEntity
│   │   ├── NotificationRepository.java
│   │   ├── NotificationSettingRepository.java
│   │   └── PushSubscriptionRepository.java
│   ├── port
│   │   ├── in
│   │   │   ├── NotificationQueryUseCase.java          # 조회
│   │   │   ├── NotificationCommandUseCase.java        # 읽음 처리
│   │   │   ├── NotificationSettingUseCase.java        # 설정 조회 / 토글
│   │   │   ├── PushSubscriptionUseCase.java           # 구독 등록 / 해제
│   │   │   └── NotificationPromptUseCase.java         # 1-8 팝업 status / dismiss
│   │   ├── out
│   │   │   └── PushSenderPort.java                    # Web Push 발송 추상화 (outbound)
│   │   ├── NotificationQueryService.java
│   │   ├── NotificationCommandService.java
│   │   ├── NotificationSettingService.java
│   │   ├── PushSubscriptionService.java
│   │   └── NotificationPromptService.java
│   ├── adapter
│   │   ├── web
│   │   │   ├── NotificationController.java
│   │   │   ├── NotificationSettingController.java
│   │   │   ├── PushSubscriptionController.java
│   │   │   ├── NotificationPromptController.java
│   │   │   └── dto/
│   │   │       ├── NotificationListResponse.java
│   │   │       ├── UnreadCountResponse.java
│   │   │       ├── NotificationSettingResponse.java
│   │   │       ├── UpdateNotificationSettingRequest.java
│   │   │       ├── RegisterPushSubscriptionRequest.java
│   │   │       ├── PushSubscriptionResponse.java
│   │   │       └── PromptStatusResponse.java
│   │   └── out
│   │       └── webpush
│   │           └── WebPushSenderAdapter.java          # PushSenderPort 구현 (VAPID + RFC 8030)
│   ├── event
│   │   └── VoteEndedNotificationHandler.java          # @EventListener(VoteEndedEvent)
│   └── exception
│       ├── NotificationNotFoundException.java
│       ├── NotificationAccessDeniedException.java
│       └── PushSubscriptionInvalidException.java
│
└── config
    └── WebPushConfig.java                             # VAPID 키쌍 빈 등록
```

> `VAPID` 라이브러리 의존성과 키 관리는 `config/WebPushConfig.java` 에 격리.

---

## 2. 단계별 구현

---

### STEP 1 — Flyway 마이그레이션 + 도메인 + Repository

**목표:** 알림 도메인 뼈대 구성

**Flyway 마이그레이션 (V4)**

`src/main/resources/db/migration/V4__notification_schema.sql`:

```sql
-- 알림
CREATE TABLE notification (
    id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id       BIGINT NOT NULL REFERENCES users(id),
    type          VARCHAR(40) NOT NULL,
    vote_id       BIGINT REFERENCES vote(id),
    title         VARCHAR(200) NOT NULL,
    body          VARCHAR(500) NOT NULL,
    thumbnail_url VARCHAR(512),
    is_read       BOOLEAN NOT NULL DEFAULT FALSE,
    read_at       TIMESTAMP WITH TIME ZONE,
    created_at    TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);
CREATE INDEX idx_notification_user_created ON notification (user_id, created_at DESC);
CREATE INDEX idx_notification_user_unread  ON notification (user_id) WHERE is_read = false;

-- 알림 설정
CREATE TABLE notification_setting (
    user_id              BIGINT PRIMARY KEY REFERENCES users(id),
    push_enabled         BOOLEAN NOT NULL DEFAULT FALSE,
    push_enabled_at      TIMESTAMP WITH TIME ZONE,
    push_disabled_at     TIMESTAMP WITH TIME ZONE,
    prompt_dismiss_count INT NOT NULL DEFAULT 0,
    last_prompt_shown_at TIMESTAMP WITH TIME ZONE,
    created_at           TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(),
    updated_at           TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);

-- 기존 회원에 대해 NotificationSetting row 생성 (운영 데이터 없음 가정)
INSERT INTO notification_setting (user_id)
SELECT id FROM users
ON CONFLICT (user_id) DO NOTHING;

-- Web Push 구독
CREATE TABLE push_subscription (
    id           BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id      BIGINT NOT NULL REFERENCES users(id),
    endpoint     VARCHAR(2048) NOT NULL UNIQUE,
    p256dh_key   VARCHAR(255) NOT NULL,
    auth_key     VARCHAR(255) NOT NULL,
    user_agent   VARCHAR(512),
    created_at   TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(),
    last_used_at TIMESTAMP WITH TIME ZONE
);
CREATE INDEX idx_push_subscription_user ON push_subscription (user_id);
```

**enum**

`notification/domain/NotificationType.java`:
```java
public enum NotificationType {
    VOTE_RESULT_PUBLISHED;
    // 향후 확장: COMMENT_REPLY, FRIEND_VOTED 등
}
```

**Notification 엔티티**

```java
@Entity
@Table(name = "notification")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class Notification extends BaseEntity {

    private Long userId;

    @Enumerated(EnumType.STRING)
    private NotificationType type;

    private Long voteId;
    private String title;
    private String body;
    private String thumbnailUrl;

    private boolean isRead;
    private Instant readAt;
    private Instant createdAt;

    public static Notification ofVoteResultPublished(
            Long userId, Long voteId, String voteTitle, String thumbnailUrl, Clock clock) {
        Notification n = new Notification();
        n.userId = userId;
        n.type = NotificationType.VOTE_RESULT_PUBLISHED;
        n.voteId = voteId;
        n.title = voteTitle;
        n.body = "투표 결과가 공개됐어요";
        n.thumbnailUrl = thumbnailUrl;
        n.isRead = false;
        n.createdAt = Instant.now(clock);
        return n;
    }

    public void markRead(Clock clock) {
        if (this.isRead) return;       // 멱등
        this.isRead = true;
        this.readAt = Instant.now(clock);
    }

    public boolean isOwnedBy(Long userId) {
        return this.userId.equals(userId);
    }
}
```

> `Notification` 은 `BaseTimeEntity`를 상속하지 않는다. `updated_at`이 의미 없고, `createdAt`은 알림 발송 시점(이벤트 처리 시점)으로 명시적 관리하는 편이 디버깅에 유리.

**NotificationSetting 엔티티**

```java
@Entity
@Table(name = "notification_setting")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class NotificationSetting extends BaseTimeEntity {

    @Id
    private Long userId;             // PK = userId (1:1)

    private boolean pushEnabled;
    private Instant pushEnabledAt;
    private Instant pushDisabledAt;
    private int promptDismissCount;
    private Instant lastPromptShownAt;

    public static NotificationSetting createDefault(Long userId) {
        NotificationSetting s = new NotificationSetting();
        s.userId = userId;
        s.pushEnabled = false;
        s.promptDismissCount = 0;
        return s;
    }

    public void enablePush(Clock clock) {
        this.pushEnabled = true;
        this.pushEnabledAt = Instant.now(clock);
    }

    public void disablePush(Clock clock) {
        this.pushEnabled = false;
        this.pushDisabledAt = Instant.now(clock);
    }

    public void recordPromptDismissed(Clock clock) {
        this.promptDismissCount += 1;
        this.lastPromptShownAt = Instant.now(clock);
    }
}
```

> `BaseTimeEntity`를 그대로 쓰면 `id` 필드가 자동 생성되어 PK 충돌. `userId`를 `@Id`로 명시적으로 지정하고, `BaseEntity`/`BaseTimeEntity` 상속 대신 직접 `@EntityListeners(AuditingEntityListener.class)` 적용을 권장. **결정 1**: `BaseTimeEntity` 상속 시 `id` 필드를 protected `final` 하지 않으므로 충돌 없음 → 그대로 상속 가능. 단 `@MapsId`나 별도 처리가 필요할 수 있어 `@DataJpaTest`로 검증 필수. **검증 결과에 따라 BaseTimeEntity 미상속 + 직접 createdAt/updatedAt 정의 fallback** 준비.

**PushSubscription 엔티티**

```java
@Entity
@Table(name = "push_subscription")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class PushSubscription extends BaseEntity {

    private Long userId;
    private String endpoint;
    private String p256dhKey;
    private String authKey;
    private String userAgent;

    private Instant createdAt;
    private Instant lastUsedAt;

    public static PushSubscription of(Long userId, String endpoint,
                                      String p256dhKey, String authKey,
                                      String userAgent, Clock clock) {
        if (endpoint == null || endpoint.isBlank()
                || p256dhKey == null || p256dhKey.isBlank()
                || authKey == null || authKey.isBlank()) {
            throw new PushSubscriptionInvalidException();
        }
        PushSubscription p = new PushSubscription();
        p.userId = userId;
        p.endpoint = endpoint;
        p.p256dhKey = p256dhKey;
        p.authKey = authKey;
        p.userAgent = userAgent;
        p.createdAt = Instant.now(clock);
        return p;
    }

    public void touch(Clock clock) {
        this.lastUsedAt = Instant.now(clock);
    }
}
```

**Repository**

`notification/domain/NotificationRepository.java`:
```java
public interface NotificationRepository extends JpaRepository<Notification, Long> {

    // 목록 조회 (커서 페이지네이션, 1개월 이내)
    @Query("""
        SELECT n FROM Notification n
         WHERE n.userId = :userId
           AND n.createdAt >= :oneMonthAgo
           AND (:cursor IS NULL OR n.id < :cursor)
         ORDER BY n.id DESC
        """)
    Slice<Notification> findPage(@Param("userId") Long userId,
                                 @Param("cursor") Long cursor,
                                 @Param("oneMonthAgo") Instant oneMonthAgo,
                                 Pageable pageable);

    long countByUserIdAndIsReadFalse(Long userId);

    @Modifying
    @Query("""
        UPDATE Notification n
           SET n.isRead = true, n.readAt = :now
         WHERE n.userId = :userId AND n.isRead = false
        """)
    int markAllAsRead(@Param("userId") Long userId, @Param("now") Instant now);
}
```

`notification/domain/NotificationSettingRepository.java`:
```java
public interface NotificationSettingRepository extends JpaRepository<NotificationSetting, Long> {
    // PK 자체가 userId 라서 findById = findByUserId
}
```

`notification/domain/PushSubscriptionRepository.java`:
```java
public interface PushSubscriptionRepository extends JpaRepository<PushSubscription, Long> {
    Optional<PushSubscription> findByEndpoint(String endpoint);
    List<PushSubscription> findAllByUserId(Long userId);
    List<PushSubscription> findAllByUserIdIn(Collection<Long> userIds);
    void deleteAllByUserId(Long userId);
    void deleteByEndpoint(String endpoint);
}
```

**예외 클래스**

`notification/exception/`:
- `NotificationNotFoundException` — 404
- `NotificationAccessDeniedException` — 403 (본인 외 알림 read 시도)
- `PushSubscriptionInvalidException` — 400

각 예외는 `getErrorCode()` 메서드를 가진다 (`NOTIFICATION_NOT_FOUND` 등).

**산출물 체크리스트 (STEP 1)**

- [ ] V4 마이그레이션 작성 + `./gradlew flywayMigrate` 통과
- [ ] `NotificationType` enum 생성
- [ ] `Notification`, `NotificationSetting`, `PushSubscription` 엔티티 생성
- [ ] Repository 3개 생성
- [ ] 예외 클래스 3개 생성
- [ ] 테스트:
    - `NotificationTest`: `ofVoteResultPublished()` / `markRead()` 멱등성 / `isOwnedBy()`
    - `NotificationSettingTest`: `createDefault()` / `enablePush()` / `disablePush()` / `recordPromptDismissed()`
    - `PushSubscriptionTest`: `of()` 정상 / endpoint 누락 / p256dhKey 누락 / authKey 누락
    - `NotificationRepositoryTest` (`@DataJpaTest`): `findPage()` 커서 페이지네이션 / 1개월 필터 / `markAllAsRead()` bulk update
    - `PushSubscriptionRepositoryTest` (`@DataJpaTest`): `findByEndpoint`, `findAllByUserIdIn`, `deleteAllByUserId`, endpoint UNIQUE 제약 위반
- [ ] `./gradlew build` 통과

---

### STEP 2 — UseCase + Service (조회 / 명령 / 설정 / 구독 / 프롬프트)

**목표:** 비즈니스 로직 인터페이스 정의 + 구현

#### 2-1. NotificationQueryUseCase

`notification/port/in/NotificationQueryUseCase.java`:
```java
public interface NotificationQueryUseCase {
    NotificationPageResult getList(Long userId, Long cursor, int size);
    long getUnreadCount(Long userId);
}

public record NotificationPageResult(
        List<NotificationView> notifications, Long nextCursor, boolean hasNext) { }

public record NotificationView(
        Long notificationId, NotificationType type, Long voteId,
        String title, String body, String thumbnailUrl,
        boolean isRead, Instant createdAt) {

    public static NotificationView from(Notification n) {
        return new NotificationView(
            n.getId(), n.getType(), n.getVoteId(),
            n.getTitle(), n.getBody(), n.getThumbnailUrl(),
            n.isRead(), n.getCreatedAt());
    }
}
```

`notification/port/NotificationQueryService.java`:
```java
@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)
public class NotificationQueryService implements NotificationQueryUseCase {

    private static final Duration ONE_MONTH = Duration.ofDays(30);

    private final NotificationRepository notificationRepository;
    private final Clock clock;

    @Override
    public NotificationPageResult getList(Long userId, Long cursor, int size) {
        Instant oneMonthAgo = Instant.now(clock).minus(ONE_MONTH);
        Slice<Notification> slice = notificationRepository.findPage(
            userId, cursor, oneMonthAgo, PageRequest.ofSize(size));

        List<NotificationView> views = slice.getContent().stream()
            .map(NotificationView::from).toList();
        Long nextCursor = slice.hasNext() && !views.isEmpty()
            ? views.get(views.size() - 1).notificationId() : null;
        return new NotificationPageResult(views, nextCursor, slice.hasNext());
    }

    @Override
    public long getUnreadCount(Long userId) {
        return notificationRepository.countByUserIdAndIsReadFalse(userId);
    }
}
```

#### 2-2. NotificationCommandUseCase

```java
public interface NotificationCommandUseCase {
    void markAsRead(Long notificationId, Long userId);
    void markAllAsRead(Long userId);

    // 이벤트 핸들러용 — 외부에서 직접 호출하지 않음
    void createBatch(List<NotificationCreateCommand> commands);
}

public record NotificationCreateCommand(
        Long userId, NotificationType type, Long voteId,
        String title, String body, String thumbnailUrl) { }
```

`notification/port/NotificationCommandService.java`:
```java
@Service
@RequiredArgsConstructor
@Transactional
public class NotificationCommandService implements NotificationCommandUseCase {

    private final NotificationRepository notificationRepository;
    private final Clock clock;

    @Override
    public void markAsRead(Long notificationId, Long userId) {
        Notification n = notificationRepository.findById(notificationId)
            .orElseThrow(NotificationNotFoundException::new);
        if (!n.isOwnedBy(userId)) throw new NotificationAccessDeniedException();
        n.markRead(clock);
    }

    @Override
    public void markAllAsRead(Long userId) {
        notificationRepository.markAllAsRead(userId, Instant.now(clock));
    }

    @Override
    public void createBatch(List<NotificationCreateCommand> commands) {
        if (commands.isEmpty()) return;
        List<Notification> notifications = commands.stream()
            .map(c -> {
                if (c.type() == NotificationType.VOTE_RESULT_PUBLISHED) {
                    return Notification.ofVoteResultPublished(
                        c.userId(), c.voteId(), c.title(), c.thumbnailUrl(), clock);
                }
                throw new IllegalArgumentException("Unsupported type: " + c.type());
            }).toList();
        notificationRepository.saveAll(notifications);
    }
}
```

#### 2-3. NotificationSettingUseCase

```java
public interface NotificationSettingUseCase {
    NotificationSettingView get(Long userId);
    NotificationSettingView updatePushEnabled(Long userId, boolean pushEnabled);
}

public record NotificationSettingView(
        boolean pushEnabled, Instant pushEnabledAt, Instant pushDisabledAt) {

    public static NotificationSettingView from(NotificationSetting s) {
        return new NotificationSettingView(
            s.isPushEnabled(), s.getPushEnabledAt(), s.getPushDisabledAt());
    }
}
```

`notification/port/NotificationSettingService.java`:
```java
@Service
@RequiredArgsConstructor
@Transactional
public class NotificationSettingService implements NotificationSettingUseCase {

    private final NotificationSettingRepository settingRepository;
    private final Clock clock;

    @Override
    @Transactional(readOnly = true)
    public NotificationSettingView get(Long userId) {
        NotificationSetting s = settingRepository.findById(userId)
            .orElseGet(() -> settingRepository.save(NotificationSetting.createDefault(userId)));
        return NotificationSettingView.from(s);
    }

    @Override
    public NotificationSettingView updatePushEnabled(Long userId, boolean pushEnabled) {
        NotificationSetting s = settingRepository.findById(userId)
            .orElseGet(() -> NotificationSetting.createDefault(userId));
        if (pushEnabled) s.enablePush(clock); else s.disablePush(clock);
        settingRepository.save(s);
        return NotificationSettingView.from(s);
    }
}
```

> `findById().orElseGet(createDefault)` 패턴은 회원가입 시점 NotificationSetting row 보장이 부재할 때를 위한 fallback. User 도메인 담당자와 협의해 회원가입 트랜잭션에서 NotificationSetting도 함께 저장하면 이 fallback은 제거 가능.

#### 2-4. PushSubscriptionUseCase

```java
public interface PushSubscriptionUseCase {
    Long register(Long userId, String endpoint, String p256dhKey,
                  String authKey, String userAgent);
    void unregisterAll(Long userId);
}
```

`notification/port/PushSubscriptionService.java`:
```java
@Service
@RequiredArgsConstructor
@Transactional
public class PushSubscriptionService implements PushSubscriptionUseCase {

    private final PushSubscriptionRepository repository;
    private final Clock clock;

    @Override
    public Long register(Long userId, String endpoint, String p256dhKey,
                         String authKey, String userAgent) {
        // 동일 endpoint가 이미 있으면 last_used_at만 갱신 (upsert)
        Optional<PushSubscription> existing = repository.findByEndpoint(endpoint);
        if (existing.isPresent()) {
            PushSubscription p = existing.get();
            p.touch(clock);
            return p.getId();
        }
        PushSubscription saved = repository.save(
            PushSubscription.of(userId, endpoint, p256dhKey, authKey, userAgent, clock));
        return saved.getId();
    }

    @Override
    public void unregisterAll(Long userId) {
        repository.deleteAllByUserId(userId);
    }
}
```

#### 2-5. NotificationPromptUseCase (1-8 팝업)

```java
public interface NotificationPromptUseCase {
    PromptStatusResult getStatus(Long userId);
    void recordDismissed(Long userId);
}

public record PromptStatusResult(boolean shouldShow, long totalParticipationCount) { }
```

`notification/port/NotificationPromptService.java`:
```java
@Service
@RequiredArgsConstructor
@Transactional
public class NotificationPromptService implements NotificationPromptUseCase {

    private final NotificationSettingRepository settingRepository;
    private final VoteQueryUseCase voteQueryUseCase;   // ★ vote BC 의존
    private final Clock clock;

    @Override
    @Transactional(readOnly = true)
    public PromptStatusResult getStatus(Long userId) {
        NotificationSetting s = settingRepository.findById(userId)
            .orElseGet(() -> NotificationSetting.createDefault(userId));

        if (s.isPushEnabled()) {
            return new PromptStatusResult(false, 0);
        }
        long count = voteQueryUseCase.countParticipationByUserId(userId);  // 회원만
        boolean shouldShow = (count == 1) || (count > 0 && (count - 1) % 10 == 0);
        return new PromptStatusResult(shouldShow, count);
    }

    @Override
    public void recordDismissed(Long userId) {
        NotificationSetting s = settingRepository.findById(userId)
            .orElseGet(() -> NotificationSetting.createDefault(userId));
        s.recordPromptDismissed(clock);
        settingRepository.save(s);
    }
}
```

> `VoteQueryUseCase.countParticipationByUserId(Long userId)` 메서드는 vote 도메인에 추가 필요. **본 PR에서 vote 도메인에 메서드만 추가**하는 작은 변경으로 처리. (vote-implementation-tasks.md의 `VoteQueryUseCase`를 한 줄 수정 + `VoteParticipationRepository.countByUserId(Long userId)` 추가)

**산출물 체크리스트 (STEP 2)**

- [ ] UseCase 인터페이스 5개 + Service 구현 5개
- [ ] DTO record 7개 (`NotificationView`, `NotificationPageResult`, `NotificationCreateCommand`, `NotificationSettingView`, `PromptStatusResult` 등)
- [ ] vote 도메인에 `countParticipationByUserId` 1줄 추가 (별도 commit으로 분리)
- [ ] 테스트:
    - `NotificationQueryServiceTest`: 페이지네이션 / 1개월 필터 / 빈 결과 시 nextCursor null
    - `NotificationCommandServiceTest`: `markAsRead` 본인 / 본인 아님(403) / 멱등성 / `markAllAsRead` 일괄 / `createBatch` 빈 리스트 / 다중 row
    - `NotificationSettingServiceTest`: `get` 신규 회원 default 생성 / `updatePushEnabled` ON / OFF 시점 갱신
    - `PushSubscriptionServiceTest`: 신규 등록 / 동일 endpoint 재등록 시 upsert / `unregisterAll`
    - `NotificationPromptServiceTest`: pushEnabled=true → false / count=1 → true / count=11 → true / count=5 → false / `recordDismissed` 카운트 증가

---

### STEP 3 — REST Controllers

**목표:** REST 엔드포인트 노출 + DTO 직렬화 + KST 변환

#### 3-1. KST 직렬화 유틸 (전역)

`common/web/KstInstantSerializer.java` (이미 vote 명세에서 정의된 것 재사용 가능):
```java
public class KstInstantSerializer extends JsonSerializer<Instant> {
    private static final ZoneOffset KST = ZoneOffset.ofHours(9);
    @Override
    public void serialize(Instant value, JsonGenerator gen, SerializerProvider serializers)
            throws IOException {
        if (value == null) { gen.writeNull(); return; }
        gen.writeString(value.atOffset(KST).toString());
    }
}
```

#### 3-2. NotificationController

`notification/adapter/web/NotificationController.java`:
```java
@RestController
@RequestMapping("/api/notifications")
@RequiredArgsConstructor
public class NotificationController {

    private final NotificationQueryUseCase queryUseCase;
    private final NotificationCommandUseCase commandUseCase;

    @GetMapping
    public NotificationListResponse getList(
            @AuthenticationPrincipal Long userId,
            @RequestParam(required = false) Long cursor,
            @RequestParam(defaultValue = "20") int size) {
        if (userId == null) throw new UnauthorizedException();
        return NotificationListResponse.from(queryUseCase.getList(userId, cursor, size));
    }

    @GetMapping("/unread-count")
    public UnreadCountResponse getUnreadCount(@AuthenticationPrincipal Long userId) {
        if (userId == null) throw new UnauthorizedException();
        return new UnreadCountResponse(queryUseCase.getUnreadCount(userId));
    }

    @PostMapping("/{notificationId}/read")
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void markAsRead(
            @PathVariable Long notificationId,
            @AuthenticationPrincipal Long userId) {
        if (userId == null) throw new UnauthorizedException();
        commandUseCase.markAsRead(notificationId, userId);
    }

    @PostMapping("/read-all")
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void markAllAsRead(@AuthenticationPrincipal Long userId) {
        if (userId == null) throw new UnauthorizedException();
        commandUseCase.markAllAsRead(userId);
    }
}
```

#### 3-3. NotificationSettingController

`notification/adapter/web/NotificationSettingController.java`:
```java
@RestController
@RequestMapping("/api/me/notification-setting")
@RequiredArgsConstructor
public class NotificationSettingController {

    private final NotificationSettingUseCase useCase;

    @GetMapping
    public NotificationSettingResponse get(@AuthenticationPrincipal Long userId) {
        if (userId == null) throw new UnauthorizedException();
        return NotificationSettingResponse.from(useCase.get(userId));
    }

    @PutMapping
    public NotificationSettingResponse update(
            @AuthenticationPrincipal Long userId,
            @RequestBody @Valid UpdateNotificationSettingRequest request) {
        if (userId == null) throw new UnauthorizedException();
        return NotificationSettingResponse.from(
            useCase.updatePushEnabled(userId, request.pushEnabled()));
    }
}

public record UpdateNotificationSettingRequest(@NotNull Boolean pushEnabled) { }
```

#### 3-4. PushSubscriptionController

`notification/adapter/web/PushSubscriptionController.java`:
```java
@RestController
@RequestMapping("/api/me/push-subscription")
@RequiredArgsConstructor
public class PushSubscriptionController {

    private final PushSubscriptionUseCase useCase;

    @PostMapping
    public PushSubscriptionResponse register(
            @AuthenticationPrincipal Long userId,
            @RequestHeader(value = "User-Agent", required = false) String userAgent,
            @RequestBody @Valid RegisterPushSubscriptionRequest request) {
        if (userId == null) throw new UnauthorizedException();
        Long subscriptionId = useCase.register(
            userId, request.endpoint(), request.p256dhKey(),
            request.authKey(), userAgent);
        return new PushSubscriptionResponse(subscriptionId);
    }

    @DeleteMapping
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void unregister(@AuthenticationPrincipal Long userId) {
        if (userId == null) throw new UnauthorizedException();
        useCase.unregisterAll(userId);
    }
}

public record RegisterPushSubscriptionRequest(
        @NotBlank String endpoint,
        @NotBlank String p256dhKey,
        @NotBlank String authKey) { }
```

#### 3-5. NotificationPromptController

`notification/adapter/web/NotificationPromptController.java`:
```java
@RestController
@RequestMapping("/api/me/notification-prompt")
@RequiredArgsConstructor
public class NotificationPromptController {

    private final NotificationPromptUseCase useCase;

    @GetMapping("/status")
    public PromptStatusResponse getStatus(@AuthenticationPrincipal Long userId) {
        if (userId == null) throw new UnauthorizedException();
        PromptStatusResult result = useCase.getStatus(userId);
        return new PromptStatusResponse(result.shouldShow(), result.totalParticipationCount());
    }

    @PostMapping("/dismissed")
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void recordDismissed(@AuthenticationPrincipal Long userId) {
        if (userId == null) throw new UnauthorizedException();
        useCase.recordDismissed(userId);
    }
}
```

> URL은 spec.md와 동일하게 `/api/me/notification-prompt-status` / `/api/me/notification-prompt-dismissed` 가 아닌 `/api/me/notification-prompt/status` / `/api/me/notification-prompt/dismissed` 형태로 묶었다. **결정 2**: spec과 plan 중 어느 쪽으로 합의할지 프론트와 의논. 본 plan에서는 RESTful sub-resource 방식을 채택. (spec.md 수정 시 플랫(flat) URL로 통일 가능 — 작업 시점에 결정)

#### 3-6. 응답 DTO 직렬화

```java
public record NotificationItem(
        Long notificationId, NotificationType type, Long voteId,
        String title, String body, String thumbnailUrl, boolean isRead,
        @JsonSerialize(using = KstInstantSerializer.class) Instant createdAt) {

    public static NotificationItem from(NotificationView v) {
        return new NotificationItem(
            v.notificationId(), v.type(), v.voteId(),
            v.title(), v.body(), v.thumbnailUrl(), v.isRead(), v.createdAt());
    }
}

public record NotificationListResponse(
        List<NotificationItem> notifications, Long nextCursor, boolean hasNext) {

    public static NotificationListResponse from(NotificationPageResult r) {
        return new NotificationListResponse(
            r.notifications().stream().map(NotificationItem::from).toList(),
            r.nextCursor(), r.hasNext());
    }
}
```

**산출물 체크리스트 (STEP 3)**

- [ ] Controller 4개 생성
- [ ] DTO 7개 생성 (Request 2 + Response 5)
- [ ] `KstInstantSerializer` 적용
- [ ] 테스트:
    - `NotificationControllerTest` (`@WebMvcTest`): 200 정상 / 401 비회원 / 페이지네이션 응답 검증 / KST 직렬화 검증
    - `NotificationSettingControllerTest`: GET / PUT 200, 401, 잘못된 body 400
    - `PushSubscriptionControllerTest`: POST 200 정상 / endpoint 누락 400 / DELETE 204 / 401
    - `NotificationPromptControllerTest`: shouldShow=true 응답 / dismissed 204

---

### STEP 4 — VAPID 설정 + Web Push 발송 어댑터

**목표:** 외부 푸시 서비스 발송 추상화 및 구현

#### 4-1. 라이브러리 선택

본 PR에서는 **`com.github.web-push-libs:webpush-java`** (Maven Central 미발행, GitHub Packages 또는 직접 빌드 필요) 또는 **`nl.martijndwars:web-push:5.x`** (Maven Central, BouncyCastle 의존) 중 후자를 채택.

`build.gradle` (또는 `build.gradle.kts`):
```gradle
implementation 'nl.martijndwars:web-push:5.1.1'
implementation 'org.bouncycastle:bcprov-jdk18on:1.78.1'
```

> Java 25 + BouncyCastle 호환성 검증 필요. 만약 BouncyCastle이 Java 25 BouncyCastle FIPS 미지원 등으로 막히면, **fallback: 직접 RFC 8030 구현** (VAPID JWT는 `java.security` 표준 ECDSA, 페이로드 암호화는 `javax.crypto` ECDH + AES-GCM). 작업 시작 시점에 의존성 build 통과 여부를 가장 먼저 확인.

#### 4-2. VAPID 키 생성 (1회성 작업)

로컬에서 1회 실행 후 결과를 `application-prod.yml` / 시크릿 매니저에 박아둠:
```bash
npm install -g web-push
web-push generate-vapid-keys
# Output:
#   Public Key:  BNc...4=
#   Private Key: kJh...wQ=
```

`application.yml`:
```yaml
webpush:
  vapid:
    public-key: ${VAPID_PUBLIC_KEY}
    private-key: ${VAPID_PRIVATE_KEY}
    subject: mailto:dev@vsapp.example.com
```

> 공개키는 프론트에도 동일하게 주입. Service Worker가 `pushManager.subscribe({ applicationServerKey: VAPID_PUBLIC_KEY })` 호출 시 사용한다.

#### 4-3. WebPushConfig

`config/WebPushConfig.java`:
```java
@Configuration
@ConfigurationProperties(prefix = "webpush.vapid")
@Getter @Setter
public class WebPushConfig {
    private String publicKey;
    private String privateKey;
    private String subject;

    @Bean
    public PushService pushService() throws GeneralSecurityException {
        Security.addProvider(new BouncyCastleProvider());
        return new PushService(publicKey, privateKey, subject);
    }
}
```

#### 4-4. PushSenderPort (outbound port)

`notification/port/out/PushSenderPort.java`:
```java
public interface PushSenderPort {
    /**
     * 단일 PushSubscription에 대해 페이로드 발송. 동기 호출.
     * @return 발송 결과 (성공 / GONE → 구독 폐기 / 일시적 실패)
     */
    SendResult send(PushSubscription subscription, PushPayload payload);

    enum SendResult { OK, GONE, RETRYABLE_ERROR }
}

public record PushPayload(
        NotificationType type,
        String title,
        String body,
        Long voteId,
        String url) {

    public String toJson() {
        return """
            {"type":"%s","title":"%s","body":"%s","data":{"voteId":%d,"url":"%s"}}
            """.formatted(type, title, body, voteId, url);
        // 실제로는 ObjectMapper 사용 권장 (이스케이프 처리)
    }
}
```

#### 4-5. WebPushSenderAdapter

`notification/adapter/out/webpush/WebPushSenderAdapter.java`:
```java
@Component
@RequiredArgsConstructor
@Slf4j
public class WebPushSenderAdapter implements PushSenderPort {

    private final PushService pushService;

    @Override
    public SendResult send(PushSubscription subscription, PushPayload payload) {
        try {
            Notification notif = new Notification(
                subscription.getEndpoint(),
                Utils.loadPublicKey(subscription.getP256dhKey()),
                Utils.loadAuthSecret(subscription.getAuthKey()),
                payload.toJson().getBytes(StandardCharsets.UTF_8)
            );
            HttpResponse response = pushService.send(notif);
            int status = response.getStatusLine().getStatusCode();
            if (status == 201 || status == 200) return SendResult.OK;
            if (status == 404 || status == 410) return SendResult.GONE;
            log.warn("Web Push unexpected status={} endpoint={}", status, subscription.getEndpoint());
            return SendResult.RETRYABLE_ERROR;
        } catch (Exception e) {
            log.error("Web Push send failed endpoint={}", subscription.getEndpoint(), e);
            return SendResult.RETRYABLE_ERROR;
        }
    }
}
```

> `nl.martijndwars.webpush.Notification` 클래스명이 본 도메인 `Notification`과 충돌하므로 import 시 alias 또는 FQCN 사용 (`webpush.Notification` 명시).

**산출물 체크리스트 (STEP 4)**

- [ ] `nl.martijndwars:web-push` + `bcprov-jdk18on` 의존성 추가 + Java 25 호환성 확인
- [ ] VAPID 키 생성 + `application.yml` 설정
- [ ] `WebPushConfig` (PushService 빈)
- [ ] `PushSenderPort` (outbound port) + `PushPayload` record
- [ ] `WebPushSenderAdapter` 구현
- [ ] 테스트:
    - `WebPushSenderAdapterTest`: WireMock으로 푸시 서비스 mocking → 201/410/500 응답 시 SendResult 매핑 검증
    - `PushPayloadTest`: JSON 직렬화 검증 (이스케이프 처리)

---

### STEP 5 — VoteEndedEvent 핸들러 (이벤트 통합)

**목표:** Vote 마감 이벤트 → 알림 row 생성 + Web Push 발송

#### 5-1. AsyncConfig 확인

vote 도메인의 `AsyncConfig`(STEP 6에서 정의)에 `notificationExecutor` 스레드 풀 추가 필요:

`config/AsyncConfig.java` (vote 명세에서 정의된 것에 추가):
```java
@Bean("notificationExecutor")
public Executor notificationExecutor() {
    ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
    executor.setCorePoolSize(2);
    executor.setMaxPoolSize(8);
    executor.setQueueCapacity(500);
    executor.setThreadNamePrefix("notif-");
    executor.initialize();
    return executor;
}
```

#### 5-2. VoteEndedNotificationHandler

`notification/event/VoteEndedNotificationHandler.java`:
```java
@Component
@RequiredArgsConstructor
@Slf4j
public class VoteEndedNotificationHandler {

    private final VoteRepository voteRepository;                       // vote BC
    private final VoteParticipationRepository voteParticipationRepository; // vote BC
    private final NotificationCommandUseCase notificationCommandUseCase;
    private final NotificationSettingRepository settingRepository;
    private final PushSubscriptionRepository pushSubscriptionRepository;
    private final PushSenderPort pushSender;

    @EventListener
    @Async("notificationExecutor")
    @Transactional
    public void on(VoteEndedEvent event) {
        // 1. Vote 정보 조회
        Vote vote = voteRepository.findById(event.voteId()).orElse(null);
        if (vote == null) {
            log.warn("Vote not found for VoteEndedEvent voteId={}", event.voteId());
            return;
        }

        // 2. 참여 회원 (user_id != null) 의 userId 목록 조회
        List<Long> participantUserIds = voteParticipationRepository
            .findUserIdsByVoteIdAndUserIdIsNotNull(event.voteId());
        if (participantUserIds.isEmpty()) return;

        // 3. Notification batch insert
        List<NotificationCreateCommand> commands = participantUserIds.stream()
            .map(uid -> new NotificationCreateCommand(
                uid, NotificationType.VOTE_RESULT_PUBLISHED,
                vote.getId(), vote.getTitle(),
                "투표 결과가 공개됐어요", vote.getThumbnailUrl()))
            .toList();
        notificationCommandUseCase.createBatch(commands);

        // 4. Web Push 발송 대상 필터: push_enabled=true 인 사용자
        List<Long> enabledUserIds = settingRepository.findAllById(participantUserIds).stream()
            .filter(NotificationSetting::isPushEnabled)
            .map(NotificationSetting::getUserId)
            .toList();
        if (enabledUserIds.isEmpty()) return;

        // 5. PushSubscription 일괄 조회 + 발송
        List<PushSubscription> subscriptions =
            pushSubscriptionRepository.findAllByUserIdIn(enabledUserIds);

        PushPayload payload = new PushPayload(
            NotificationType.VOTE_RESULT_PUBLISHED,
            "투표 결과가 공개됐어요",
            vote.getTitle(),
            vote.getId(),
            "/votes/" + vote.getId() + "/result"
        );

        List<PushSubscription> goneSubs = new ArrayList<>();
        for (PushSubscription sub : subscriptions) {
            PushSenderPort.SendResult result = pushSender.send(sub, payload);
            if (result == PushSenderPort.SendResult.GONE) {
                goneSubs.add(sub);
            }
            // OK / RETRYABLE_ERROR 는 별도 처리 없음 (RETRYABLE은 추후 재시도 큐 도입)
        }

        // 6. 폐기된 구독 정리
        if (!goneSubs.isEmpty()) {
            pushSubscriptionRepository.deleteAll(goneSubs);
            log.info("Cleaned up {} stale push subscriptions", goneSubs.size());
        }
    }
}
```

> `findUserIdsByVoteIdAndUserIdIsNotNull` 메서드는 vote 도메인의 `VoteParticipationRepository`에 추가 필요. STEP 2에서 `VoteQueryUseCase.countParticipationByUserId`와 함께 vote 측 추가 작업으로 묶어 1회만 PR 분기.

#### 5-3. 트랜잭션 경계

`@EventListener` + `@Async` + `@Transactional`의 조합은 **이벤트 발행 트랜잭션과 별개의 새 트랜잭션**에서 실행된다. 따라서 vote close 트랜잭션이 커밋된 후 알림이 생성됨이 보장된다.

> 만약 알림 처리 중 오류가 발생해도 vote close는 이미 커밋되어 롤백되지 않는다. 알림 누락 시 재시도 정책은 별도 PR에서 결정 (현재는 best-effort).

#### 5-4. 테스트 전략

- **단위 테스트** (`VoteEndedNotificationHandlerTest`): Mockito로 의존성 mocking → 이벤트 수신 시 `createBatch` 호출 / push 발송 호출 검증
- **통합 테스트** (`NotificationIntegrationTest`, `@SpringBootTest`):
  1. 회원 A, B, C가 vote 참여
  2. 회원 A는 push_enabled=true + PushSubscription 등록
  3. `VoteEndedEvent` 직접 발행 (`ApplicationEventPublisher.publishEvent`)
  4. Awaitility로 비동기 완료 대기
  5. 검증:
     - `notification` 테이블에 A, B, C 3건 row 존재
     - `WebPushSenderAdapter` mock에 A의 endpoint로 1번 호출됨
     - 410 응답 시 PushSubscription 삭제 검증

**산출물 체크리스트 (STEP 5)**

- [ ] `AsyncConfig`에 `notificationExecutor` 스레드 풀 추가
- [ ] `VoteEndedNotificationHandler` 구현
- [ ] vote 도메인 측 추가 메서드:
    - `VoteParticipationRepository.findUserIdsByVoteIdAndUserIdIsNotNull(Long voteId)`
    - `VoteQueryUseCase.countParticipationByUserId(Long userId)` + Service 구현
- [ ] 테스트:
    - `VoteEndedNotificationHandlerTest` (Mockito): 정상 / vote 없음 / 참여자 0명 / push_enabled=false / GONE 응답 시 폐기
    - `NotificationIntegrationTest` (`@SpringBootTest` + Awaitility): end-to-end 흐름 검증

---

## 3. Flyway 마이그레이션 정리

| 파일 | 도메인 |
|------|--------|
| `V1__init_schema.sql` | users, token (기존) |
| `V2__chat_schema.sql` | vote(mock), vote_participation(mock), chat_message, chat_room_unread, User 컬럼 추가 |
| `V3__vote_schema.sql` | vote/vote_participation 확장, vote_option, vote_emoji_reaction, guest_free_vote, users(gender/birth_date) |
| **`V4__notification_schema.sql`** | **notification, notification_setting, push_subscription** |

---

## 4. 테스트 전략 요약

| 레이어 | 도구 | 대상 |
|--------|------|------|
| Domain | JUnit 5 + Mock Clock | 팩토리 메서드, 도메인 검증, 멱등성 |
| Application | JUnit 5 + Mockito | UseCase 비즈니스 로직, 권한 분기, 1-8 팝업 판정 |
| Persistence | `@DataJpaTest` | 커서 페이지네이션, bulk update, UNIQUE 제약 |
| Controller | `@WebMvcTest` | 요청/응답 직렬화, 401/403, KST 시간 변환 |
| Web Push | WireMock | 푸시 서비스 mock 응답 → SendResult 매핑 |
| Integration | `@SpringBootTest` + Awaitility | VoteEndedEvent → 알림 생성 + 푸시 발송 end-to-end |

---

## 5. 구현 순서 요약

```
STEP 1  V4 마이그레이션 + 도메인 + Repository
STEP 2  UseCase + Service 5종 (Query / Command / Setting / Subscription / Prompt)
STEP 3  REST Controllers 4종 + DTO + KST 직렬화
STEP 4  VAPID 설정 + WebPushSenderAdapter (PushSenderPort)
STEP 5  VoteEndedNotificationHandler (@EventListener + @Async)
        + vote 도메인 측 메서드 2개 추가
```

---

## 6. 진행 중 의사결정 필요 시

다음 항목은 진행 중 막히면 사용자에게 질문하라:

1. **Web Push 라이브러리 호환성** — `nl.martijndwars:web-push` + BouncyCastle이 Java 25에서 빌드 통과하는지 가장 먼저 확인. 막히면 fallback으로 직접 RFC 8030 구현 (작업량 +2~3일).
2. **NotificationSetting row 생성 시점** — 회원가입 트랜잭션 / `UserCreatedEvent` 핸들러 / lazy fallback 중 무엇을 채택할지 User 도메인 담당자와 협의. 본 plan은 lazy fallback 채택.
3. **1-8 팝업 트리거 카운트** — "10회 진행할 때마다"의 정확한 의미가 (1, 11, 21, ...) 인지 (10, 20, 30, ...) 인지 PM과 확정. 본 plan은 (1, 11, 21, ...) 채택.
4. **Notification BaseTimeEntity 상속 가능 여부** — `userId`를 `@Id`로 쓸 때 `BaseTimeEntity.id` 와 충돌하는지 STEP 1 시작 시 `@DataJpaTest`로 검증. 충돌 시 직접 `@EntityListeners(AuditingEntityListener.class)` 적용.
5. **알림 페이로드 JSON 포맷** — 프론트(Service Worker) 측과 페이로드 키 컨벤션 확정 후 `PushPayload.toJson()` 변경.
6. **재시도 정책** — RETRYABLE_ERROR 발생 시 무시할지 별도 큐(Spring Retry / DB outbox)로 재발송할지. 본 PR은 best-effort 무시. 별도 PR로 분리.

위 항목 외에는 본 문서의 결정사항을 따르고, 모호한 부분은 가장 보수적인 선택 (예외 던지기, 멱등 처리, 누락 시 로그만 남기기)을 한다.

---

## 7. 최종 검증

전체 STEP 완료 후:

```bash
./gradlew clean build
./gradlew test
./gradlew jacocoTestReport
```

다음 시나리오를 통합 테스트로 추가:

1. **알림 생명주기**: 회원이 vote 참여 → vote 마감 (스케줄러 트리거) → 알림 row 생성 + Web Push 발송 → `GET /api/notifications`로 조회 → 알림 카드 클릭(`POST .../{id}/read`) → `unread-count` 감소 확인
2. **푸시 토글 ON/OFF**: 토글 ON → PushSubscription 등록 → vote 마감 → 푸시 발송 검증 → 토글 OFF → PushSubscription 삭제 검증 → 다음 vote 마감 시 푸시 발송 X
3. **1-8 팝업 트리거**: 회원 첫 vote 참여 → `GET .../prompt/status` 호출 → `shouldShow=true` → "나중에 할게요" 클릭(`POST .../prompt/dismissed`) → 5번 더 참여 → `shouldShow=false` → 11번째 참여 후 `shouldShow=true`
4. **GONE 처리**: 만료된 endpoint로 푸시 발송 시도 → 410 응답 → PushSubscription row 삭제 확인
5. **비회원 차단**: 비회원으로 모든 알림 API 호출 시 401 일관 응답 검증
